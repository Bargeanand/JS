const object={
    userName:"anand",
    rollNo:305
}
for(const key in object)
{
    console.log(`key's are:${key} values are:${object[key]}`)
}
for(const key in object)
{
    console.log(`key's are:${key} values are:${object.key}`)
}